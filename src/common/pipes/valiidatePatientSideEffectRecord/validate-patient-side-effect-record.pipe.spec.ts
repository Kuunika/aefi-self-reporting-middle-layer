import { ValidatePatientSideEffectRecordPipe } from './validate-patient-side-effect-record.pipe';

describe('ValidatePatientSideEffectRecordPipe', () => {
  it('should be defined', () => {
    expect(new ValidatePatientSideEffectRecordPipe()).toBeDefined();
  });
});
